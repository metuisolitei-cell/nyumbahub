package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light / Minimalism Colors (Blue-600 accents, emerald M-Pesa)
val PrimaryGreen = Color(0xFF2563EB) // Clean minimal main blue
val SecondaryAqua = Color(0xFF0D9488) // Soft Teal
val AccentAmber = Color(0xFFD97706) // Elegant Amber

// Modern Minimalism Canvas & Surface Tokens (Clean light mode)
val SlateBackground = Color(0xFFF7F9FC) // Soft light grey-blue background
val SlateCard = Color(0xFFFFFFFF) // Crisp white card surface
val SlateBorder = Color(0xFFE2E8F0) // Subtle line border (Slate-200)

// Support Utilities (High Contrast Minimal text & Semantic status)
val MpesaGreen = Color(0xFF059669) // Safaricom Emerald Green
val CriticalRed = Color(0xFFEF4444) // Clean Red (Rose-500)
val PureWhite = Color(0xFF0F172A) // High contrast slate-900 for primary text
val MutedText = Color(0xFF64748B) // Slate-500 for secondary body text
val WarmGold = Color(0xFFD97706) // Modern warm Gold (Amber-600)

